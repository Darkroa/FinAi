Next, add a live margin calculator preview below the Lot Size and Leverage fields that shows the required margin in real-time as you adjust the inputs

Botpage
In Strategy if user selected live in Bot Direction auto should now be available only buy or sell so if user select live buy or sell enter the market immediately and other configuration still apply 
The bot default leverage should be 1:200 the highest plus user leverage option Remove the Max down line and add box too 
In Finevent bot 
 Add color and circle to the icon at the top of 
No FinEventAI bots running
Your plan allows 50 bots · click "Add Bot" to get started
Under add the Add bot button below remove from current place the collapse should be available for only active bot

In dashboard Activity Center 
NEW display number of new News and Trade display number or trade 

Use Replit dummy api key for Ai so if grok OpenAI key not available use Replit dummy key remove the available key not subscription yes the chat flot button remove it and login page make add home page url to the logo so user can go back to landing page from the login page to all other page make static so when user tap input the webpage don't zoom 

Trade
Let's check the trading logic its should match live execution logic you buy market go up you make profit or gose down you lose live balance updates login and add lot size too 

The error or status or or snackbar pop up change the design to something like this uScaffoldMessenger.of(context).showSnackBar(
  SnackBar(
    content: Row(
      children: [
        Icon(Icons.check_circle, color: Colors.white),
        SizedBox(width: 12),
        Text("Saved"),
      ],
    ),
    backgroundColor: Colors.green[700],
    behavior: SnackBarBehavior.floating,   // Makes it float nicely
    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
    duration: Duration(seconds: 3),
    action: SnackBarAction(
      label: "Dismiss",
      textColor: Colors.white,
      onPressed: () {}, // optional
    ),
    dismissDirection: DismissDirection.horizontal, // Swipe support
  ),
);

Ask me before you start and finish before you checkout 
task required Now make you use all import and no typescript eorro 
Now ask me if you understand before start 