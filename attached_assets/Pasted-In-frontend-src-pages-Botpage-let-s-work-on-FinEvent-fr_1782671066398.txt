In frontend/src/pages/Botpage 
let's work on FinEvent front and backend logic
At the top of the page the STOP ALL  button only displays when FinBot is running or both of the correct the logic it should display as long as any of them is running 
First the card should not be collapsable if not bot is running and the add Bot button at the middle is not displaying fix it let it render static like the FinBot card 
Now  in the FinEvent if the bot to trade according to user configuration if user set like 5 trades if bot detect 19 it will trade 19 including leverage and other 
And for the FinEvent picker (let's change the logic from input and user picker selector that supports multiple selection for execution ) let's make the bot run base on user configuration 
(NOTE IF USER SELECT MULTIPLE PICKER LIKE BTC-usd eth-usd IT SHOULD COUNT AS 2 bot runnnig) and add open position chat for FinEvent open position just like FinBot lets {/* Live summary chips */} render it should display like the margin calculator render style

FinBot card
the FinBot is okay let's add improvements first check the Inline Edit Panel state if it's correct and let's check if the {/* Number of Trades */} input is what the bot trade work let's update the backend login and in the start bot config for FinBot let's % percentage per bot execution trade from the start bot capital  let's say i started a bot with 2000000 And set 5 times trade execution the % 10 box the bot should trade with 200000 for the 5time and the   Execution should route using lots size leverage or margin (those figures are for explanation it should depend on user config just saying )
  In  {/* Config summary grid + action buttons */} especially the config summary let's render like {/* Margin Calculator — card style */} the calculator output style

NOTE
The FinBot and FinEvent config have same style but different so the Ui blend and it's the FinEvent card you will be update to use the FinBot card and config style and REMOVE ANY (PAPER TRADING) routes  only trade from user balance or connected exchang