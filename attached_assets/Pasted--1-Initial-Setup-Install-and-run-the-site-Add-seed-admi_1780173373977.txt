### 1. Initial Setup
- Install and run the site.
- Add seed admin credentials to the database:
  - Email: AdminfinAi@gmail.com
  - Password: FineAdminpass1

SYSTEM UPDATE 

Please update the AI Bot name module to the new "Fin Bot" specifications detailed below. Implement changes across the Frontend UI components, State Management, and Backend Trading Logic.
Under balance card
reduces the size of Today's P&L & unrealized P&L and below a tiny long box like deposit size show Portfolio Value, active bots number and open position number  dashboard Activity center 
FIN BOT
AI Bot
Offline
FinEvent
Offline (space to much between status and offline and red dot live green dot)  The Chat fin remove the overlays fixed button change very contact email to supportfinaibot@gmail.com
---

### 1. CORE BRANDING & GLOBAL NAVIGATION
* **Rename Module:** Change all instances of "AI Bot" or "AI Bot or Bot Palace" to "Fin Bot".
* **Theme Alignment:** Apply unified ecosystem styles/colors to all Fin Bot components.
* **At the top "FinEventAI Status: 
* **Button Layout:** Near the "Add Bot" button, add a sibling button titled "FinEventAI Bots" that matches its styling.

---

### 2. "CONFIGURE NEW BOT" FORM OVERHAUL
Modify the creation form components. Remove all continuous slider/dragging lines and replace them with precise numeric inputs or clickable option boxes:

* **Capital Input:** user input add 
* Lot Size Input:** Input box with a default value of `1`. Allowed custom range is `1` to `100`.
* **Risk Per Trade Removal:** Completely remove the "Risk per trade" input field. The bot must rely entirely on the Lot Size input.
* **Take Profit (%):** Input box with a default value of `50`. Allowed custom range is `5` to `200`.
* **Stop Loss (%):** Input box with a default value of `30`. Allowed custom range is `5` to `100`.
* **Execution Cooldown Delay:** Implement a mandatory minimum delay logic. The bot must wait at least 40 seconds before executing any initialized trade.
* **Leverage Selectors:** Replace the dragging line with an array of individual clickable option grid boxes. The selectable values are exactly:
  [1:1], [1:5], [1:10], [1:25], [1:50], [1:100], [1:200], [1:500], [1:1000], [1:1200]

---

### 3. BACKEND BROKER MATH & VALIDATION LOGIC
Implement validation rules to prevent users with small capital allocations from opening unsustainably large positions:

* **Standard Math Constraints:** Treat 1 Lot as 100,000 units of the base asset.
* **Margin Check Formula:** 
  Required Margin = (Lot Size * 100,000) / Selected Leverage
* **Enforcement Rule:** Before saving a configuration or executing an automated trade, check if `Required Margin > Allocated Capital`. If true, block the submission and throw an error: "Insufficient Capital for this Lot Size/Leverage configuration."
* **P&L Scaling:** Scale all profit and loss dollar calculations symmetrically with the chosen lot size volume (larger lot size = larger cash value movement per pip).

---

### 4. LIVE TRADE LOG & HISTORY VISUALS
* **in Ai bot Live Trade Log:** "Live Trade Log" component to render exactly the 10 most recent trade records.
* **Navigation Trigger:** Add a clean "View All" action button directly underneath the 10-log display list.
* **Target Redirect:** Configure the "View All" button to route users seamlessly to the dedicated "AI Trade History" ledger view page.

Ai chat error 
I'm having trouble connecting to my AI engine right now. Please make sure the GROK_API_KEY or OPENAI_API_KEY is configured. Error: No module named 'langchain_core' it should not display display this use this and modify it I echo name The Im fin probably under mentaince can you check back 

Please don't change bot design login apart from the task required Now make you use all import and no typescript eorro 
Now ask me if you understand before start 
