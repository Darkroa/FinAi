In Tradepage 
Before Pair selector after header add buy and sell button at the sell left lot size middle buy right that execute instantly (it should have the hide&unhide feature  )so Pair in selector card  remove Balance & Open P&L from the card and reduce the lent of the card  Below before tradingview chat add the open position card use the  dashbroad open position card style it display when position is open the card should calculate total open position and total position value add realized&unrealized  for only trades
Make the buy and sell tabs below chatfin collapse able and hide & unhide  futures like chatfin (the hide and unhide should be in smae place like chatfin )

Now in Dashboard the (Portfolio Value Active Bots and open position) the card show only active bot trades tho the open position is there but not counting numbers of open Traders position and  the portfolio value should be calculating total position value of Trade , AiBot  Finevent, Vps and asset  together and the unrealized and realized  should also calculate the total  realized&UnRl of 5

In backup folder The exportdb and seeddb is kit working because process skipped existing data and ran into critical data type mismatches. Here is exactly what happened to every single database table:
## ❌ Tables with Fatal Errors & Skipped Rows
These tables failed to import specific rows because the data formats in your backup file do not match  current database schema. 

users: Contains 1 row. It was skipped because of a Python/ORM error: can't adapt type 'dict'. The script tried to pass a structured dictionary object into a database column that wasn't expecting it.
* api_keys: Contains 1 row. It was skipped because your database expects a json type for the allowed_scopes column, but your backup file tried to insert a PostgreSQL array text format: ARRAY['read', ...].
* notifications: Contains 66 rows. Five critical rows (IDs 1 through 5) failed and were skipped because the backup file formats read_by_user_ids as an integer array (ARRAY[1]), but your active database schema requires a json data type. So in backup folder update the both of them seed and export if the py export is run it should export all databases details users email hash password and export everything and the seed should insert it make it match the  database schema work with the code 

Backend job in session and main py and rouute or anywhere that use os.getenv like db connection should read from env so you'll add load_dotenv where needed
Just incase I want to use outside db con 

Make sure you finish Any migration and to main.py so it can auto create without wiping the the db Make sure no npx tsc --noEmit 2>&1 build error production 